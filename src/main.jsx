import React from 'react'
import ReactDOM from 'react-dom/client'
import CalAIClone from './CalAIClone.jsx' // Make sure the path matches where your component is stored

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CalAIClone />
  </React.StrictMode>,
)